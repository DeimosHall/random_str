[package]
name = "random_str"
version = "1.1.0"
edition = "2024"
rust-version = "1.85.0"
description = "Generate random strings, chars, booleans, and integers."
license = "MIT"
repository = "https://github.com/DeimosHall/random_str"

[dependencies]
rand = "0.10.0"
